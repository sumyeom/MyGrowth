spring.application.name: MyGrowth

spring:
  ai:
    openai:
      api-key: ${OPENAI_KEY}
      chat:
        base-url: "https://generativelanguage.googleapis.com/v1beta/openai/"
        options:
          model: "gemini-2.0-flash-lite"
          temperature: 0.7
        completions-path: "/chat/completions"